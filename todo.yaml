#// TODO FRONT:
  #// Crée la section de recherche sur le site
  #// Quand on clique sur un genre, faire en sorte de voir tout les films du genre
  #// Quand on clique sur un artiste, faire en sorte de voir tout les films de l'artiste
  #// Quand on clique sur un real, faire en sorte de voir tout les films du real
  #// Crée pagination pour 20 film
  #// Terminer la page de présentation du film
  #// Pour la bande annonce, ouverture d'un popup avec vidéo.js

#// TODO BACKEND:
#// FilmsRepository ou GenreRepository : Crée une requête qui afficher les films similaire en fonction du genre du films.
#// Faire un random sur la requete getFilmsBLockbuster
#// Ajouter une count dans linkvideofilms en disant que X manque des liens
#// Crée une recherche dans les genres, realisateurs, acteurs
#// Crée une pagination pour les genres, realisateurs, acteurs (limit 20)
#// Ajouter les realisateurs + les genres dans admin.films.index
#// Crée la partie Séries
#// Chaque séries à une saison avec plusieurs épisodes, aussi elle peut avoir plusierus saison
#// Crée l'entité séries : dans cette entitée mettre : name, relation avec genre+acteurs+realisateurs, number episode, number saison,
#// Crée entitée video_séries : dans cette entitée mettre : url, relation avec séries, saison_number, episode_number
#// Ajouter (13) films 3)


#// TERMINER :
#// Faire toute la génération sur le front.
