{% extends 'admin/base.html.twig' %}

{% block title %}New Acteur{% endblock %}

{% block body %}
    <main class="main">
        <ol class="breadcrumb">
        </ol>
        {% for message in app.flashes('success') %}
            <div class="alert alert-success">
                {{ message }}
            </div>
        {% endfor %}
        <div class="container-fluid">
            <div class="row">
                <div class="col-lg-12">
                    <div class="card">
                        <div class="card-header">
                            <i class="fa fa-align-justify"></i>Crée un nouveau realisateur</div>
                        <div class="card-body">
                            {{ include('admin/realisateur/_form.html.twig') }}
                            <a href="{{ path('admin.realisateur.index') }}">Revenir à la liste</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>

    </main>
{% endblock %}
